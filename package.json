{
  "name": "tpl_weltspiegel",
  "version": "2.2.3",
  "description": "Joomla-Template für den Weltspiegel Cottbus",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/weltspiegel-cottbus/tpl_weltspiegel.git"
  },
  "type": "module",
  "scripts": {
    "build": "vite build",
    "package": "node .build/package.mjs",
    "release": "npm run build && npm run package",
    "changelog": "changelogen --output CHANGELOG.md --noAuthors",
    "release:patch": "changelogen --release --patch --push --noAuthors",
    "release:minor": "changelogen --release --minor --push --noAuthors",
    "release:major": "changelogen --release --major --push --noAuthors"
  },
  "devDependencies": {
    "changelogen": "^0.6.2",
    "vite": "^8.0.0"
  }
}
